// suppress inspection "AsciiDocLinkResolve" for whole file
:ghos: https://github.com/verronpro/asciidoc
:wrkf: {ghos}/actions/workflows
:docs: https://asciidoc.verron.pro
:blog: https://verron.pro

= AsciiDoc Modules for Office-stamper

image:{wrkf}/integrate-os.yml/badge.svg[Build Status,link={wrkf}/integrate-os.yml]
image:{wrkf}/pages.yml/badge.svg[Build Status,link={wrkf}/pages.yml]

== Introduction

This project provides a suite of modules for handling AsciiDoc content within the Office-stamper ecosystem.
It enables bidirectional conversion between AsciiDoc and DOCX, as well as high-quality rendering to HTML, SVG, and PNG.

It is designed to be used as a set of libraries to integrate AsciiDoc processing into Java applications, especially those using Office-stamper for document generation.

== Key Features

* **Bidirectional DOCX Conversion**: Convert AsciiDoc to DOCX and vice versa (with support for `AlternateContent` and `pict` elements).
* **Lightweight Rendering**: Render AsciiDoc to HTML, SVG, and Text without needing a full Asciidoctor installation.
* **Compiler API**: High-level API for complex conversion tasks and PNG export.
* **Documentation Previews**: AsciidoctorJ extension for embedding previews in your own documentation.
* **Modular Architecture**: Use only the components you need (requires Java 25).
* **Rich Iconography**: Built-in support for SVG icons.

== Modules

This repository is structured into several modules:

* {docs}/asciidoc-core[Core] – Foundation data model and parsing logic.
* {docs}/asciidoc-converters[Converters] – Lightweight rendering to HTML, SVG, and Text.
* {docs}/asciidoc-docx[DOCX] – Conversion between AsciiDoc and Microsoft Word (DOCX).
* {docs}/asciidoc-compiler[Compiler] – High-level API and PNG export utilities.
* {docs}/asciidoc-preview[Preview] – AsciidoctorJ extension for embedding previews.

== Architecture

[mermaid]
----
graph TD
    subgraph AsciiDoc Parent
        core[asciidoc-core]
        conv[asciidoc-converters]
        docx[asciidoc-docx]
        comp[asciidoc-compiler]
        ext[asciidoc-preview]
    end

    conv --> core
    docx --> core
    comp --> core
    comp --> conv
    comp --> docx
    ext --> comp
----

== Documentation

A comprehensive documentation is available at {docs}[{docs}].

== Author

Joseph Verron - {blog}/about.html[About Me]

== License

MIT License
